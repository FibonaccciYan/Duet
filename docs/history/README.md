# Historical records

Files here describe previous versions, cleanup decisions and measured runs.
Their version labels and old paths are intentionally preserved for provenance;
they are not current import or launch instructions.

Use `../../README.md`, `../PACKAGE_LAYOUT.md` and `../runtime_integration.md`
for the current source layout. Generated benchmark results remain outside the
source package and are not rewritten during migration.
